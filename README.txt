download bach, ml.* and flucoma packages

classification.maxpat
1/load a sound by clicking on "replace"
2/double click buffer~to check your sound is valid
3/red button

markov3
1/load a sound by clicking on "replace"
2/ trigger segmentation (onsets), until you get the number of slices you want
3/ compute mapping
4/ hit "trigger" to start, de-toggle panick to stop
5/ change the top right settings (which class/cluster, how many items within that class, input 2 integers, then click out of the box)
6/ lowering the "decay" slider will add polyphony
7/ augmenting the "attack" slider will avoid clicks, but also potentially dammage the attacks
